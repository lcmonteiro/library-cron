name: Cron CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: build directory
      run: mkdir ./build & cd ./build
    - name: cmake
      run: cmake ..
    - name: make check
      run: make check
